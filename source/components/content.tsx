import React from 'react';
import {Box} from 'ink';

type Props = {};

/**
 * The content to display
 */
const content = ({}: Props) => {
	return <Box height={'100%'}></Box>;
};

export default content;
